# CUDA graph and larger-prefill profile. Apply this first and benchmark it
# before enabling speculative decoding.
ENFORCE_EAGER=0
VERIFY_CUDA_GRAPHS=1
COMPILATION_CONFIG='{"cudagraph_mode":"FULL_AND_PIECEWISE"}'
MAX_NUM_BATCHED_TOKENS=32768
MTP_SPECULATIVE_TOKENS=0
